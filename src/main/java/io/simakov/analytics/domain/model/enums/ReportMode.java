package io.simakov.analytics.domain.model.enums;

public enum ReportMode {
    /**
     * Count MRs created within the requested period
     */
    CREATED_IN_PERIOD,
    /**
     * Count MRs merged within the requested period
     */
    MERGED_IN_PERIOD
}
